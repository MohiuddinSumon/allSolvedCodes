#include<bits/stdc++.h>
using namespace std;
int main()
{




    return 0;
}

/*
 string a;

    int t;
    cin>>t;
    while(t--)
    {
        int fr[26] = {0};
        cin>>a;
        string b = "";
        for(int i=0; i<a.size(); i++)
        {
            fr[a[i]-'a']++;
            //cout<<fr[a[i]-'a']<<endl;
            //if(a[i]!=a[i+1])b+=a[i];
            //else i++;
        }
        for(int i=0; i<26; i++){
                if(fr[i]%2 ==1)b+=i+'a';
                //cout<<fr[i]<<' '<<i+'a'<<endl;

//
//            if(fr[a[i]-'a']%2 != 0){
//                    cout<<fr[a[i]-'a']<<endl;
//                    b+=i+'a';
//                    cout<<b<<endl;
//            }
        }
        //if(a[a.size()-1]!=a[a.size()-2])b += a[a.size()-1];
        sort(b.begin(),b.end());
        cout<<b<<endl;
    }
    */
